import PropTypes from 'prop-types';
import React from 'react';

const Button = props => {
    return (
        <div>
            Hello
        </div>
    );
};

Button.propTypes = {
    
};

export default Button;